1.python 函数调用传参问题
对于不可变对象 int,float,long,tuple,str 等基本类型，传过去的是值，这个很好理解，跟c一样。
它们的值本身就不能够改变，如果在被调函数里面修改了值，必然重新新建一个对象，来存，而原对象自然不变。

可变对象就不一样了，它默认传引用，即传地址过来。。。。字典，列表，自定义类属于这种类型。。。。。。
这些对象的赋值语句也是一样，   是引用式赋值，若要深度拷贝，使用copy()..........